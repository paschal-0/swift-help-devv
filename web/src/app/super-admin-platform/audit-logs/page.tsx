import { SuperAdminPlaceholderPage } from "../components/SuperAdminPlaceholderPage";

export default function SuperAdminAuditLogsRoute() {
  return (
    <SuperAdminPlaceholderPage
      title="Audit Logs"
      description="Inspect admin actions, security-sensitive events, configuration changes, and system access history."
    />
  );
}
