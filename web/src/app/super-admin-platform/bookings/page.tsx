import { SuperAdminPlaceholderPage } from "../components/SuperAdminPlaceholderPage";

export default function SuperAdminBookingsRoute() {
  return (
    <SuperAdminPlaceholderPage
      title="Bookings & Consultations"
      description="Track booking requests, reschedules, live sessions, completed consultations, cancellations, and expired appointments."
    />
  );
}
