import { SuperAdminPlaceholderPage } from "../components/SuperAdminPlaceholderPage";

export default function SuperAdminShiftsRoute() {
  return (
    <SuperAdminPlaceholderPage
      title="Shift Management"
      description="Oversee open shifts, filled slots, attendance, missed shifts, funding state, and organization staffing demand."
    />
  );
}
