import { SuperAdminPlaceholderPage } from "../components/SuperAdminPlaceholderPage";

export default function SuperAdminSettingsRoute() {
  return (
    <SuperAdminPlaceholderPage
      title="Settings"
      description="Control platform configuration, admin defaults, security settings, and operational rules."
    />
  );
}
